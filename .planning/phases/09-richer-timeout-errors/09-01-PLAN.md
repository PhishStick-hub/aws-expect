---
phase: 09-richer-timeout-errors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aws_expect/_utils.py
  - aws_expect/exceptions.py
  - tests/test_utils_truncate.py
  - tests/test_utils_format.py
autonomous: true
requirements:
  - ERR-03

must_haves:
  truths:
    - "_truncate_value correctly truncates lists > 50 items and values > 500 chars"
    - "_format_timeout_error produces Expected:/Actual: sections when fields present"
    - "_format_timeout_error omits sections when expected/actual is None"
    - "WaitTimeoutError has expected=None and actual=None as class-level defaults"
  artifacts:
    - path: "aws_expect/_utils.py"
      provides: "_truncate_value and _format_timeout_error functions"
      exports: ["_truncate_value", "_format_timeout_error"]
    - path: "aws_expect/exceptions.py"
      provides: "WaitTimeoutError with expected/actual class-level attributes"
      contains: "expected: Any = None"
    - path: "tests/test_utils_truncate.py"
      provides: "Unit tests for _truncate_value"
      min_lines: 40
    - path: "tests/test_utils_format.py"
      provides: "Unit tests for _format_timeout_error"
      min_lines: 40
  key_links:
    - from: "aws_expect/exceptions.py"
      to: "aws_expect/_utils.py"
      via: "from aws_expect._utils import _format_timeout_error"
      pattern: "from aws_expect\\._utils import.*_format_timeout_error"
    - from: "aws_expect/_utils.py"
      to: "builtins"
      via: "pure functions — no aws_expect imports"
      pattern: "def _truncate_value|def _format_timeout_error"
---

<objective>
Implement the shared `_truncate_value` and `_format_timeout_error` helpers in `_utils.py`, add `expected`/`actual` class-level defaults to `WaitTimeoutError`, and write unit tests for both helpers.

Purpose: All timeout error messages will be consistently formatted through these shared functions. This plan lays the foundation — without it, no subclass can produce the new `Expected:`/`Actual:` format.
Output: `_utils.py` with two new functions, `exceptions.py` with updated base class, two new test files.
</objective>

<execution_context>
@/Users/ivan_shcherbenko/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ivan_shcherbenko/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-richer-timeout-errors/09-CONTEXT.md
@.planning/phases/09-richer-timeout-errors/09-RESEARCH.md
</context>

<interfaces>
<!-- Key existing patterns the executor needs -->

From aws_expect/_utils.py (existing helpers — same file gets new functions):
```python
# Existing imports used by _utils.py
from aws_expect.exceptions import StopConditionError, StopConditionMetError

# Existing helpers (add new functions alongside these):
def _deep_matches(actual: dict[str, Any], expected: dict[str, Any]) -> bool: ...
def _matches_entries(item: dict[str, Any], entries: dict[str, Any]) -> bool: ...
def _compute_delay(poll_interval: float) -> int: ...
def _build_waiter_config(timeout: float, poll_interval: float) -> WaiterConfigTypeDef: ...
def _check_stop_condition(state, stop_when, resource_id, start, timeout) -> dict | None: ...
```

From aws_expect/exceptions.py (WaitTimeoutError base class — add class-level attrs):
```python
class WaitTimeoutError(Exception):
    """Base class for all wait timeout errors."""
    timeout: float
```

Target signature for _truncate_value (per D-09):
```python
def _truncate_value(value: Any) -> str:
    """Format value with truncation for error messages. Never raises."""
```

Target signature for _format_timeout_error (per D-04):
```python
def _format_timeout_error(
    resource_desc: str,
    expected: Any,
    actual: Any,
    timeout: float,
) -> str:
    """Produce structured timeout error message with Expected:/Actual: sections."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _truncate_value with tests</name>
  <files>aws_expect/_utils.py, tests/test_utils_truncate.py</files>
  <read_first>
    - aws_expect/_utils.py — understand existing helper patterns and import style
    - tests/test_exceptions.py — understand existing test patterns (pytest classes, assertions)
    - .planning/phases/09-richer-timeout-errors/09-CONTEXT.md — D-07, D-08, D-09 truncation rules
  </read_first>
  <behavior>
    - Test 1: `_truncate_value(None)` → `"None"`
    - Test 2: `_truncate_value([1, 2, 3])` → `"[1, 2, 3]"` (≤ 50 items, no truncation)
    - Test 3: `_truncate_value(list(range(100)))` → shows first 50 items, then `... (50 more items not shown)`
    - Test 4: `_truncate_value("short")` → `"'short'"` (repr, ≤ 500 chars)
    - Test 5: `_truncate_value("x" * 1000)` → shows first 500 chars of repr, then `... (value truncated, showing first 500 of NNNN chars)`
    - Test 6: `_truncate_value((1, 2))` → works for tuples (same as lists)
    - Test 7: `_truncate_value(42)` → `"42"` (non-collection, short repr)
    - Test 8: `_truncate_value({"key": "value"})` → `"{'key': 'value'}"` (dict, short repr)
  </behavior>
  <action>
    Create `tests/test_utils_truncate.py` with a `class TestTruncateValue` containing the 8 tests above using pytest.
    
    Then implement `_truncate_value(value: Any) -> str` in `aws_expect/_utils.py` with a Google-style docstring. The function:
    1. Returns `"None"` for `None` input (string, not None object)
    2. For `list`/`tuple` with ≤ 50 items: returns `repr(value)`
    3. For `list`/`tuple` with > 50 items: returns `repr(value[:50])` + newline + `... (N more items not shown)` where N = len(value) - 50
    4. For all other types where `len(repr(value))` ≤ 500: returns `repr(value)`
    5. For all other types where `len(repr(value))` > 500: returns `repr(value)[:500]` + newline + `... (value truncated, showing first 500 of NNNN chars)` where NNNN = `len(repr(value))`

    Per D-09: use `repr()` for both the threshold check AND the output content. Do NOT use `str()`.
    Never raises — always returns a string.
    
    Add `from typing import Any` if not already imported in `_utils.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_utils_truncate.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_utils_truncate.py` exists with `class TestTruncateValue`
    - `aws_expect/_utils.py` contains `def _truncate_value(value: Any) -> str:`
    - All 8 tests pass
    - `uv run ruff check aws_expect/_utils.py tests/test_utils_truncate.py` passes
    - `uv run ruff format aws_expect/_utils.py tests/test_utils_truncate.py` passes
  </acceptance_criteria>
  <done>_truncate_value implemented with tests passing; all truncation thresholds (50 items, 500 chars) verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _format_timeout_error with tests</name>
  <files>aws_expect/_utils.py, tests/test_utils_format.py</files>
  <read_first>
    - aws_expect/_utils.py — find where to add _format_timeout_error after _truncate_value
    - .planning/phases/09-richer-timeout-errors/09-CONTEXT.md — D-04 (message format), D-05 (how subclasses call it)
  </read_first>
  <behavior>
    - Test 1: both `expected` and `actual` are `None` → single line: `"Timed out after 10.0s waiting for my-resource"` (no Expected:/Actual: block)
    - Test 2: `expected={"key": "val"}`, `actual=None` → has `Expected:` section with formatted value, no `Actual:` section
    - Test 3: `expected=None`, `actual={"key": "val"}` → no `Expected:` section, has `Actual:` section
    - Test 4: both `expected` and `actual` present → both sections, separated by blank line
    - Test 5: large expected value → truncation applied in output (verify `_truncate_value` is called)
    - Test 6: `timeout=30.5` → message includes `30.5s`
    - Test 7: `resource_desc="s3://my-bucket/key"` → message includes the exact resource_desc string
  </behavior>
  <action>
    Create `tests/test_utils_format.py` with a `class TestFormatTimeoutError` containing the 7 tests above. Tests should call `_format_timeout_error` and assert on the returned string.

    Then implement `_format_timeout_error(resource_desc: str, expected: Any, actual: Any, timeout: float) -> str` in `aws_expect/_utils.py` right after `_truncate_value`. Google-style docstring. The function:
    1. Opens with: `f"Timed out after {timeout}s waiting for {resource_desc}"`
    2. If BOTH `expected` and `actual` are `None`: return just the first line (per D-04)
    3. Otherwise: append blank line, then sections:
       - If `expected is not None`: `"Expected:"` + newline + `f"  {_truncate_value(expected)}"`
       - If `actual is not None`: if expected was also present add blank line between sections, then `"Actual:"` + newline + `f"  {_truncate_value(actual)}"`
    4. Join all lines with `"\n"` and return.

    Per D-13: this is a module-level function, not a method on any class. Keeps it importable without circular imports.
    Per D-04: uses 2-space indent for the formatted values under Expected:/Actual: labels.
  </action>
  <verify>
    <automated>uv run pytest tests/test_utils_format.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_utils_format.py` exists with `class TestFormatTimeoutError`
    - `aws_expect/_utils.py` contains `def _format_timeout_error(resource_desc: str, expected: Any, actual: Any, timeout: float) -> str:`
    - All 7 tests pass
    - Both None → no Expected:/Actual: block
    - One None → only the non-None section appears
    - Both present → both sections with blank line separator
  </acceptance_criteria>
  <done>_format_timeout_error implemented with tests passing; correct Expected:/Actual: formatting for all combinations</done>
</task>

<task type="auto">
  <name>Task 3: Add expected/actual class-level defaults to WaitTimeoutError</name>
  <files>aws_expect/exceptions.py</files>
  <read_first>
    - aws_expect/exceptions.py — lines 11-19 (WaitTimeoutError base class)
    - .planning/phases/09-richer-timeout-errors/09-CONTEXT.md — D-01 (expected/actual as class-level attrs), D-03 (classes without fields inherit None)
    - aws_expect/_utils.py — confirm _format_timeout_error import path
  </read_first>
  <action>
    In `aws_expect/exceptions.py`, modify the `WaitTimeoutError` class (currently lines 11-18):

    Add two class-level attributes after `timeout: float`:
    ```python
    expected: Any = None
    actual: Any = None
    ```

    Update the class docstring to document these new attributes:
    ```
    Attributes:
        timeout: The configured timeout (float).
        expected: What the waiter expected to find. Defaults to ``None`` for
            subclasses that don't track expectations.
        actual: What was actually observed. Defaults to ``None`` for
            subclasses that don't track actual state.
    ```

    Also add `_format_timeout_error` and `_truncate_value` to the `_utils.py` module docstring if one exists, or confirm they're importable. No imports change needed in exceptions.py — subclasses will import `_format_timeout_error` in Plan 02.

    Per D-01: these are class-level attributes, not instance attributes set in `__init__`. Subclasses inherit them automatically.
    Per D-03: `S3WaitTimeoutError`, `LambdaWaitTimeoutError`, and other classes without explicit expected/actual will inherit `None` from the base class — no changes needed to their constructors.
  </action>
  <verify>
    <automated>uv run python -c "from aws_expect.exceptions import WaitTimeoutError; e = WaitTimeoutError(); assert e.expected is None; assert e.actual is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `WaitTimeoutError.expected` is `None` (class-level attribute)
    - `WaitTimeoutError.actual` is `None` (class-level attribute)
    - `WaitTimeoutError` docstring documents both attributes
    - All existing tests still pass: `uv run pytest tests/ -v --timeout=120`
  </acceptance_criteria>
  <done>WaitTimeoutError has expected/actual class-level defaults; all existing 167+ integration tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase deals with string formatting of exception messages — no data crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `_truncate_value` / `_format_timeout_error` | accept | Exception messages already contain resource IDs and data via `repr()`. Truncation limits (50 items, 500 chars) reduce exposure but the values are already present in `repr()` output throughout the codebase. No PII or secrets in test resource names. |
| T-09-02 | Denial of Service | `_truncate_value` large input handling | mitigate | `len(repr(value))` on very large nested structures could be expensive. The 500-char slice on repr output limits formatting cost. `repr()` of deeply nested dicts naturally bounds output — no recursive explosion path exists. |
</threat_model>

<verification>
- `uv run pytest tests/test_utils_truncate.py tests/test_utils_format.py -v` — all helper tests pass
- `uv run python -c "from aws_expect.exceptions import WaitTimeoutError; assert WaitTimeoutError.expected is None; assert WaitTimeoutError.actual is None"`
- `uv run pytest tests/ -v --timeout=120` — all existing tests still pass (no regression)
</verification>

<success_criteria>
- `_truncate_value` handles all truncation cases (None, small list, large list, short value, long value)
- `_format_timeout_error` produces correctly structured messages for all expected/actual combinations
- `WaitTimeoutError` base class exposes `expected` and `actual` as class-level defaults
- Zero regression: all existing 167+ integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-richer-timeout-errors/09-01-SUMMARY.md`
</output>
